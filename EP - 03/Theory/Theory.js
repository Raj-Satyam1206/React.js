// If you want to know how to run the project for development build and production build, we have to go to the "scripts" section of package.json


/* 
const heading = React.createElement(
    "h1",{ id:"heading"},"Hello World"
);
console.log(heading);




const jsxHeading = <h1>React</h1>
console.log(jsxHeading);



- Both of them will print the same object
*/


// JSX(transpiled) => Babel transpiles it to React.createElement => React Element => JS Object => HTML Element(render)



//JSX is different than "HTML in JS" . In HTML, we use "class" as attribute but in JSX , class is taken as a keyword, so we use "className".